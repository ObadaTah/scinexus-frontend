function SendButton() {
    const onClick = () => {};

    return (
        // TBI : To be implemented
        <button onClick={onClick}>
            <img src="/Send.svg" alt="Send" />
        </button>
    );
}

export default SendButton;
